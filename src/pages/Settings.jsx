import { motion } from 'framer-motion';
import { categories, priorityOptions } from '../utils/task';

export default function Settings({ settings, updateSettings, clearCompleted, resetData }) {
  const select = (key) => (event) => updateSettings({ [key]: event.target.value });
  return <motion.div className="page settings-page" initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }}><div className="page-title"><p className="eyebrow">Make Toki feel like yours</p><h1>Settings</h1></div><section className="settings-section"><h2>Appearance</h2><label className="settings-row"><span><strong>Theme</strong><small>Light, dark, or your device setting.</small></span><select value={settings.theme} onChange={select('theme')}><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></label></section><section className="settings-section"><h2>New tasks</h2><label className="settings-row"><span><strong>Default priority</strong><small>Used when you quickly add a task.</small></span><select value={settings.defaultPriority} onChange={select('defaultPriority')}>{priorityOptions.map((item) => <option key={item}>{item}</option>)}</select></label><label className="settings-row"><span><strong>Default category</strong><small>Keep new tasks in the right place.</small></span><select value={settings.defaultCategory} onChange={select('defaultCategory')}>{categories.map((item) => <option key={item}>{item}</option>)}</select></label></section><section className="settings-section"><h2>Data</h2><div className="settings-row"><span><strong>Clear completed tasks</strong><small>Removes only tasks you have finished.</small></span><button className="button button-ghost" onClick={() => window.confirm('Clear all completed tasks?') && clearCompleted()}>Clear completed</button></div><div className="settings-row"><span><strong>Reset local data</strong><small>Restores the small starter list.</small></span><button className="button danger-button" onClick={() => window.confirm('Reset all local Toki data?') && resetData()}>Reset data</button></div></section></motion.div>;
}
